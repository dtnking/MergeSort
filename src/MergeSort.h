#ifndef _MERGESORT_H
#define _MERGESORT_H
#include <stdlib.h>
#include <stdio.h>
#include <stdint.h>

void MergeSort(uint8_t arr[],int l, int r);
void Merge(uint8_t arr[],int l,int r,int m);

#endif // _MERGESORT_H
